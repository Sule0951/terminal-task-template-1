# Hello World (React + TypeScript)

Create a React component at `/app/src/App.tsx` that renders the text:

```
Hello, Terminal Tasks!
```

The file must export the component as its default export, and rendering it must
produce output containing exactly `Hello, Terminal Tasks!`.
